这个题乍一看以为是回溯法求结果，但是仔细一想也不需要求具体的字母序列，所以应该是
用动态规划算法。
这个题还有个隐含条件：就是所有的数字都应该被使用，所以一个数字不可能以0开头；
                                     也不能有0未被使用。
所以比如说：11的结果是2；110的结果是1；
所以字符串中如果有连续两个0，结果就是0；
如果一个0前面的字符是1，2才符合结果；
否则结果也只能还是0；
剩下的就是一般的动态规划转移方程。