这道题普通的暴力解法就不再赘述了。
下面主要说的是单调栈的方法：
单调栈是用来解决这类题的常见方法；
单调栈可以用来找到当前元素后续的第一个最大（最小）元素；
具体方法是：
（1）进来一个元素时判断这个元素与栈顶元素的大小关系；
（2）如果比当前元素大，说明这个元素是栈顶元素数组中后续元素中第一个最大的元素；这时候要进行出栈操作；然后用当前元素，继续比较出栈后下一个栈顶元素。
（3）如果比当前元素小，这时候进行入栈操作。
（4）之后继续遍历数组。