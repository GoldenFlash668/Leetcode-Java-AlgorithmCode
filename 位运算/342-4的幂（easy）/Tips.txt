解法一：
由二次幂题中得 x&（x-1）== 0 可以判断一个数是否为2的整数次幂
4的幂与2的幂区别在于 4的幂的数转化为二进制中 1 的位置在偶数位；
所以将数与(101010...10)2​ 用十六进制表示为 ：(aaaaaaaa)16做&运算，结果为0则是4的整数次幂。
解法二：
如果数字为 4 的幂 x=4a，则 a=log⁡4x=1/2​log2​x 应为整数，那么我们检查 log⁡2x 是否为偶数就能判断 x 是否为 4 的幂。
解法二也可直接求：
return (Math.log10(n) / Math.log10(4)) % 1 == 0;
这个解法适用于所有数的次幂。